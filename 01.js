// HenryChallenge - HackerRank
// https://www.youtube.com/watch?v=vYPLwOuz9DM&ab_channel=ederEngine

// 1. En partes
// Escribe una función que permita dividir un arreglo de enteros en varias partes de manera que cada parte tenga como máximo una cantidad definida de elementos recibida por parámetro

// Ejemplo:
// partir([1, 2, 3, 4, 5], 2)
// debería retornar
// ([1, 2], [3, 4], [5])

// Descripción de la funcion partir()
// Debe retornar un arreglo de arreglos, cada arreglo sera a su vez un arreglo de enteros, Recibe como parámetros: 
// arreglo: Un arreglo de enteros
// cantidad: Un entero

// Completa la funcion partir a continuación
// La función debe retornar un ARREGLO DE ARREGLOS
// La función acepta un ARREGLO DE ENTEROS ‘arreglo’ y un ENTERO “cantidad” como parámetros
// No modifiques nada por fuera del cuerpo de esta función

function partir(arreglo, cantidad){
	// Tu codigo aqui:

}
























